<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Product</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  </head>
  <body class="flex-col">
    <div class="flex">
      <nav class="w-1/8"><%- include("./nav.ejs") %></nav>
      <div class="flex-1 flex-col bg-[url('/picture/adminHome.png')] bg-cover">
        <div>
          <p class="text-5xl font-bold text-white text-center pt-5">Sản phẩm mã <%= product.id %></p>
        </div>
        <div class="mt-[30px] ms-9">
          <div class="max-w-6xl mx-auto bg-gray-700 text-white rounded-2xl shadow-lg overflow-hidden flex flex-col md:flex-row">
            <div class="md:w-1/2">
              <img src="<%= product.picture %>" alt="<%= product.name %>" class="w-full h-full object-cover" />
            </div>
            <div class="p-6 md:w-1/2 flex flex-col justify-between">
              <h2 class="text-3xl font-bold mb-4"><%= product.name %></h2>
              <p class="text-lg mb-4"><%= product.description %></p>
              <div class="flex flex-col gap-2">
                <p>Thương hiệu: <%= product.Brand.name %></p>
                <p>Chất liệu: <%= product.Material.name %></p>
                <p>Thể loại: <%= product.Category.name %></p>
                <p>Nguồn gốc: <%= product.Origin.name %></p>
                <p>Đơn giá: <%= product.unitPrice.toLocaleString() %> ₫</p>
                <p>Số lượng: <%= product.quantity %></p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <footer><%- include("./footer.ejs") %></footer>
  </body>
  <script>
    document.getElementById('add').addEventListener('click', (e) => {
      location.href = '/admin/product/new';
    });
  </script>
</html>
